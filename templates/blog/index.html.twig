{% extends 'base.html.twig' %}

{% block title %}TomTroc - Accueil{% endblock %}

{% block main %}
<section class="hero text-center py-5 bg-light">
    <h1 class="display-4">📚 Rejoignez nos lecteurs passionnés</h1>
    <p class="lead">Échangez vos livres, vos idées, vos mondes.</p>
    <a href="{{ path('book_index') }}" class="btn btn-primary btn-lg mt-3">Commencer</a>
</section>

<section class="latest-books py-5">
    <h2 class="text-center mb-4">Les derniers livres ajoutés</h2>
    <div class="row justify-content-center">
        {% for book in books %}
            <div class="col-md-3 mb-4">
                <div class="card shadow-sm">
                    <img src="{{ book.coverUrl }}" class="card-img-top" alt="{{ book.title }}">
                    <div class="card-body">
                        <h5 class="card-title">{{ book.title }}</h5>
                        <p class="card-text">{{ book.author }}</p>
                        <a href="{{ path('book_show', {id: book.id}) }}" class="btn btn-outline-primary btn-sm">Voir</a>
                    </div>
                </div>
            </div>
        {% else %}
            <p class="text-center">Aucun livre pour le moment.</p>
        {% endfor %}
    </div>
</section>

<section class="how-it-works py-5 bg-light">
    <h2 class="text-center mb-4">Comment ça marche ?</h2>
    <div class="row text-center">
        <div class="col-md-4">
            <span class="fs-1">📖</span>
            <p>Ajoute ton livre</p>
        </div>
        <div class="col-md-4">
            <span class="fs-1">🔍</span>
            <p>Explore les livres disponibles</p>
        </div>
        <div class="col-md-4">
            <span class="fs-1">💬</span>
            <p>Contacte et échange</p>
        </div>
    </div>
</section>
{% endblock %}
